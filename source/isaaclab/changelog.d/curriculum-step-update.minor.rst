Added
^^^^^

* Added opt-in per-control-step curriculum updates through ``CurriculumTermCfg.update_mode="step"`` and
  ``CurriculumManager.compute_step()``. Existing curriculum terms retain their reset-triggered default.
