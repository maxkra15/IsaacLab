# Copyright (c) 2022-2026, The Isaac Lab Project Developers (https://github.com/isaac-sim/IsaacLab/blob/main/CONTRIBUTORS.md).
# All rights reserved.
#
# SPDX-License-Identifier: BSD-3-Clause

[build-system]
requires = ["setuptools>=70.0,<82.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "isaaclab-visualizers"
version = "0.1.1"
description = "Visualizer backends for Isaac Lab (Kit, Newton, Rerun, Viser)."
license = {text = "BSD-3-Clause"}
authors = [{name = "Isaac Lab Project Developers"}]
maintainers = [{name = "Isaac Lab Project Developers"}]
keywords = ["robotics", "simulation", "visualization"]
requires-python = ">=3.12"
dependencies = [
    "isaaclab",
    "numpy",
]

[project.urls]
Homepage = "https://github.com/isaac-sim/IsaacLab"
Repository = "https://github.com/isaac-sim/IsaacLab"

[project.optional-dependencies]
kit = []
# Every Newton declaration in the repo must use the same extra spec (`newton[sim]`).
# Pip resolves a direct-URL requirement once per URL: if any package declares bare
# `newton @ file://...` while another declares `newton[sim] @ file://...`, the first
# resolution wins and silently drops the `[sim]` extra.
newton = [
    "warp-lang",
    "newton[sim] @ file:///home/maximiliank/Work/newton-coupled",
    "PyOpenGL-accelerate",
    "imgui-bundle>=1.92.5",
]
rerun = [
    "newton[sim] @ file:///home/maximiliank/Work/newton-coupled",
    "rerun-sdk>=0.29.0",
]
viser = [
    "newton[sim] @ file:///home/maximiliank/Work/newton-coupled",
    "viser>=1.0.16",
]
all = [
    "imgui-bundle>=1.92.5",
    "newton[sim] @ file:///home/maximiliank/Work/newton-coupled",
    "PyOpenGL-accelerate",
    "rerun-sdk>=0.29.0",
    "viser>=1.0.16",
    "warp-lang",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["isaaclab_visualizers", "isaaclab_visualizers.*"]

[tool.setuptools.package-data]
"*" = ["*.pyi"]
